{
  "name": "design-system",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:docs": "turbo dev --filter=docs  --filter=ui",
    "dev:playground": "turbo dev --filter=playground  --filter=ui",
    "build": "turbo build",
    "build:ui": "turbo build --filter=ui",
    "build:docs": "turbo build --filter=docs"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
